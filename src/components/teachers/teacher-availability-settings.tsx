"use client";

interface TeacherAvailabilitySettingsProps {
  organizationId: string;
}

export function TeacherAvailabilitySettings({ organizationId }: TeacherAvailabilitySettingsProps) {
  return (
    <div>
      {/* Settings-specific availability view */}
      <h2>Teacher Availability Settings</h2>
      {/* Add your settings UI here */}
    </div>
  );
} 